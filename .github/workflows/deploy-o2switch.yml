# DÉSACTIVÉ — o2switch filtre le SSH (port 22) par adresse IP.
# Les runners GitHub ne peuvent pas s'y connecter (timeout / no key).
# Utilisez : Deploy o2switch (FTPS)

name: Deploy o2switch (SSH — désactivé)

on:
  workflow_dispatch:

jobs:
  disabled:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "Utilisez le workflow Deploy o2switch (FTPS)"
